#include <stdio.h>
#include "event.h"

/*
   This main() is ONLY for testing Member 1's part on its own.
   When you merge with your teammates, delete this file and
   call eventMenu() from the team's real main.c instead.
*/
int main()
{
    eventMenu();
    return 0;
}
